# MongoDB Connection String
MONGO_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/whisp

# Server Port (default: 5000)
# Important: Make sure your API client (Postman, frontend, etc.) uses this port
PORT=5000

# JWT Secret Key for token generation
# Use a strong, random secret in production
JWT_SECRET=your_secret_key_here

# Frontend app origin for CORS
# Set this to your deployed frontend URL in production
CLIENT_URL=http://localhost:5173
